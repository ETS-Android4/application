<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gttime.android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@drawable/app_logo_main"
        android:label="GT Time"
        android:roundIcon="@drawable/app_logo_main"
        android:supportsRtl="true"
        android:theme="@style/Theme">
        <activity android:name = ".view.activity.MainActivity"
            android:label = "GT Time">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name = ".view.effect.Pop"
            android:theme = "@style/Custom.App.Pop"></activity>
    </application>

</manifest>